/**
 * 离线批处理逻辑包（大数据平台的正确归宿）。
 *
 * <p>本包内的逻辑本质是「离线批处理」——从海量派生观测（t_kp_derived_obs）中
 * 共现沉淀规律（题型库聚合）、批量重判、漂移先验。这些逻辑<b>不要求实时</b>、
 * 数据量大、无限长尾，正确归宿是大数据平台（Spark/Flink 等离线批处理作业）。
 *
 * <p><b>逻辑归宿标注：大数据平台。</b>
 * 当前项目为纯 Java DDD 后端、暂未接入大数据平台，故这些逻辑先以
 * {@code @Scheduled} 周期任务在后端<b>过渡实现</b>。未来接大数据平台时：
 * <ol>
 *   <li>用大数据作业读 {@code t_kp_derived_obs}，写 {@code t_kp_question_type(_kp)}；</li>
 *   <li>删除本包对应的 Java 实现；</li>
 *   <li>数据表与在线解析管线②（读题型库先验）保持不变。</li>
 * </ol>
 *
 * <p><b>边界约束</b>：本包逻辑只做「obs → 题型库」的离线计算，不掺入任何实时依赖，
 * 保证未来替换成本最低。
 */
package com.ai.edu.application.service.batch;
